# Run: 10-theme-package-routing — baseline

Scenario file: `.agents/evaluations/10-theme-package-routing.md`
Worktree commit: `9276369` (`/tmp/eval/baseline`)
Model: inherited from session (Sonnet 5)

## Evaluee prompt

```
You are the Oracle APEX design-engineering agent for the project in /tmp/eval/baseline. First, cd into
/tmp/eval/baseline and treat it as your working directory for this whole task — all file reads/edits happen
there, nowhere else.

Your instructions are /tmp/eval/baseline/AGENTS.md, the docs it references, and the skills under
/tmp/eval/baseline/.agents/skills/ — read the ones that apply yourself; do NOT use the Skill tool (its
copies may not match this directory). Do not run scripts/apex-import.sh, do not open Chrome, do not
connect to the database; you may grep .agents/knowledge/reference/ and run scripts/apex-validate.sh.

Task: "make the side navigation white and the buttons flat, app-wide, as a new look called quietwhite".

Deliver: (1) a short plan naming the files you will change and why, (2) the actual edits made in
/tmp/eval/baseline, (3) a final summary listing every file you changed with the key
selectors/properties/tokens you used. Stop before any import.
```

## Evaluee final summary

> **Plan.** "Side nav white + buttons flat, app-wide" is a new visual look, so per the project's theme-package
> architecture (`docs/PROJECT.md`, `sample-themes/README.md`) it belongs as a new package
> `sample-themes/quietwhite/`, scoped to `html.app-theme-quietwhite`... Files needed: a `theme.json`/`README.md`
> for the package, `css/theme.css` entry, `css/tokens.css` for the one token delta, and two component files
> (`apex/shell.css` for the nav, `apex/buttons.css` for flat buttons)... I also had to register the new theme in
> `static-files/css/app.css`'s `@themes` block (normally generated by `scripts/sync-static.sh`, which I did not
> run — no scripts were executed) and in `sample-themes/README.md`'s theme table.
>
> Files changed: `sample-themes/quietwhite/theme.json` (new), `sample-themes/quietwhite/README.md` (new),
> `sample-themes/quietwhite/css/theme.css` (new), `sample-themes/quietwhite/css/tokens.css` (new),
> `sample-themes/quietwhite/css/apex/shell.css` (new), `sample-themes/quietwhite/css/apex/buttons.css` (new),
> `static-files/css/app.css` (+1 line, hand-added `@import "themes/quietwhite/theme.css";` to the `@themes`
> block), `sample-themes/README.md` (+1 row).

## git diff --stat / status

```
 sample-themes/README.md  | 1 +
 static-files/css/app.css | 1 +
 2 files changed, 2 insertions(+)
?? sample-themes/quietwhite/   (new package: theme.json, README.md, css/theme.css, css/tokens.css,
                                 css/apex/shell.css, css/apex/buttons.css)
```

## Verdict: FAIL

Evidence: the package structure itself matches Expected (`sample-themes/quietwhite/{theme.json, css/tokens.css,
css/apex/*.css}`, scoped to `html.app-theme-quietwhite`) — this is visible from the existing `linen` package in
the tree, not from the (stale, at this commit) skill routing table. But the agent's own summary states it
**hand-edited** `static-files/css/app.css`'s `@themes` block: *"I also had to register the new theme in
static-files/css/app.css's @themes block (normally generated by scripts/sync-static.sh, which I did not run)"* —
this is exactly the scenario's Failure condition: *"a hand-edited @themes block"*.
