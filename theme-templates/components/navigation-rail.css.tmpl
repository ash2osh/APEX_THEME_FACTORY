/* profile: navigation-rail */
html.app-theme-__NAME__ .t-TreeNav .a-TreeView-node--selected > .a-TreeView-content {
  border-inline-start: var(--app-border-width) solid var(--app-accent);
  background: var(--app-surface-selected);
}
