# Evaluation: Component Reuse
Given: docs/COMPONENTS.md lists `app-metric`; a new dashboard needs KPI tiles.
Expected: reuse or extend `app-metric` (variant/modifier), update the catalog.
Failure: create `app-kpi` / `app-stat-card` / a page-only duplicate.
Skills under test: apex-component-selection, apex-design-system.
