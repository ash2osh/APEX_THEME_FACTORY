# Run: 06-component-reuse — current

Scenario file: `.agents/evaluations/06-component-reuse.md`
Worktree commit: `b0aa923` (`/tmp/eval/current`)
Model: inherited from session (Sonnet 5)

Note: this repo's `docs/COMPONENTS.md` registry was actually empty (`_(none yet)_`) at this commit, so to give
the scenario's premise ("docs/COMPONENTS.md lists app-metric") a real artifact to reuse, a minimal fixture —
`static-files/css/components/app-metric.css`, `static-files/js/components/app-metric.js`, and a matching
registry row/contract in `docs/COMPONENTS.md` — was seeded into `/tmp/eval/current` immediately before this run
and removed by the standard `git checkout -- . && git clean -fd` reset afterward (same as any evaluee edit).

## Evaluee prompt

```
You are the Oracle APEX design-engineering agent for the project in /tmp/eval/current. First, cd into
/tmp/eval/current and treat it as your working directory for this whole task — all file reads/edits happen
there, nowhere else.

Your instructions are /tmp/eval/current/AGENTS.md, the docs it references, and the skills under
/tmp/eval/current/.agents/skills/ — read the ones that apply yourself; do NOT use the Skill tool (its
copies may not match this directory). Do not run scripts/apex-import.sh, do not open Chrome, do not
connect to the database; you may grep .agents/knowledge/reference/ and run scripts/apex-validate.sh.

docs/COMPONENTS.md already lists an `app-metric` component (a KPI tile:
static-files/css/components/app-metric.css + static-files/js/components/app-metric.js, Alpine name
`appMetric`). Read it first.

Task: A new dashboard page needs several KPI tiles (e.g. "Total Orders", "Revenue This Month", "Open
Tickets"). Add what's needed to support that.

Deliver: (1) a short plan naming the files you will change and why, (2) the actual edits made in
/tmp/eval/current, (3) a final summary listing every file you changed with the key
selectors/properties/tokens you used. Stop before any import.
```

## Evaluee final summary

> `docs/COMPONENTS.md` already documented `app-metric`... but investigation showed it was never actually wired
> up ... Fix the wiring... add a `.app-metric-row` layout helper so several tiles can sit in one region... new
> page demonstrating the pattern with the three requested tiles [using `x-data="appMetric({...})"` three times],
> not a new component. `docs/COMPONENTS.md` — update the registry.

## git diff --stat / status (evaluee edits, beyond the seeded fixture)

```
 applications/ut/application.apx                                 | 1 +
 applications/ut/pages/p00501-dashboard.apx                       | new file
 applications/ut/shared-components/static-files.apx               | regenerated by sync-static.sh
 docs/COMPONENTS.md                                                | updated (wiring note, page ref)
 static-files/css/app.css                                          | 1 +
 static-files/css/components/app-metric.css                        | + .app-metric-row helper
```

## Verdict: PASS

Evidence: no duplicate KPI component was created (no `app-kpi`, no `app-stat-card`, no page-only inline
duplicate) — the new dashboard page reuses `appMetric(...)` three times and extends `app-metric.css` with a
`.app-metric-row` layout modifier, which is exactly the Expected "reuse or extend (variant/modifier)" path.
`docs/COMPONENTS.md` was updated. No Failure condition triggered.
